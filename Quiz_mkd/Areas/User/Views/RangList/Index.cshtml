@model RangListVM

<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">Ранг листа за @Model.RangList.Event.Name</h2>
        </div>
    </div>

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                
                <th>Одговор</th>
                <th>Точен</th>
                        <th></th>
               
            </tr>
        </thead>
        <tbody>
          @*   @foreach (var item in Model)
            {
                <tr>
                    <td>@item?.Text</td>
                    @if (item?.isCorrect == true)
                    {
                        <td>Точен</td>
                    }
                    else
                    {
                        <td>Неточен</td>
                    }

                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a  asp-area="Admin" asp-controller="Answer" asp-action="Edit" asp-route-id="@item?.Id" class="btn btn-primary mx-2">
                                Едитирај
                            </a>
                          
                        </div>

                    </td>

                </tr>
            } *@

        </tbody>

    </table>
